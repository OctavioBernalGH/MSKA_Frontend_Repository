/*Screen size*/
    var width = window.innerWidth;
    var height = window.innerHeight;
    //Seting the parameters on CSS styles
    document.documentElement.style.setProperty('--my-screenwidth', width);
    document.documentElement.style.setProperty('--my-screenheight', height);
